# rueckblick-extension-chrome has NO server/runtime secrets.
# The bridge token is issued by the desktop app during pairing and stored in
# chrome.storage.local at runtime — never in a file, never in this repo.
# This file exists only to document that there is nothing to configure here.
